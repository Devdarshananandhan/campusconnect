# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/campusconnect?retryWrites=true&w=majority

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-in-production

# Elasticsearch Configuration (Optional - falls back to MongoDB if not available)
# For local development, install and run Elasticsearch, or use Elastic Cloud
ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_API_KEY=your-api-key-here (if using Elastic Cloud)

# JWT Secret (if using JWT in future)
JWT_SECRET=your-jwt-secret-key-change-in-production
